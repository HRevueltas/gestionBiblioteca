# Inicia un proyecto npm
npm init -y

# Instala dependencias de producción
npm install express promise-mysql dotenv

# Instala dependencias de desarrollo
npm install --save-dev @babel/cli @babel/core @babel/node @babel/preset-env morgan nodemon
